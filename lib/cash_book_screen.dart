import 'package:flutter/material.dart';
class CashBookScreen extends StatelessWidget{const CashBookScreen({super.key});@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('Cash Book')),body:const Center(child:Text('Cash received, cash paid and expenses can be reviewed here.')));}
