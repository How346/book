import 'package:flutter/material.dart';
class BillingScreen extends StatelessWidget{const BillingScreen({super.key});@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('Billing')),body:const Center(child:Text('Create invoices with customer, items, discount, tax and payment status.')));}
