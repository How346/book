name: digital_khata
description: Offline-first Digital Khata and small business manager.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  path_provider: ^2.1.4
  provider: ^6.1.2
  shared_preferences: ^2.5.3
  local_auth: ^2.3.0
  flutter_local_notifications: ^19.4.1
  intl: ^0.20.2
  pdf: ^3.11.3
  printing: ^5.14.2
  share_plus: ^11.0.0
  file_picker: ^10.2.0
  csv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
